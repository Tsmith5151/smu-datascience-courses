install.packages(c("brew", "countrycode",
"devtools", "dplyr",
"ggplot2", "googleVis",
"knitr", "MCMCpack",
"repmis", "RCurl",
"rmarkdown", "texreg",
"tidyr", "WDI",
"xtable", "Zelig"))
install.packages("ZeligBayesian",
repos = "http://r.iq.harvard.edu/",
type = "source")
dir <- getwd()
setwd(dir)
getwd()
dir
# Author: Trace Smith
library(plyr)
library(gdata)
setwd("/Users/tracesmith/Desktop/SMU/Github/DoingDataScience/Unit6_Assignment")
#Save as CSV File
bk <- read.csv("Dataset/rollingsales_queens.csv",skip=4,header=TRUE, pattern="BOROUGH")
## Check the data
head(bk)
summary(bk)
str(bk) # Very handy function!
# Author: Trace Smith
library(plyr)
library(gdata)
setwd("/Users/tracesmith/Desktop/SMU/Github/DoingDataScience/Unit6_Assignment")
#Save as CSV File
bk <- read.csv("Dataset/rollingsales_queens.csv",skip=4,header=TRUE)
## Check the data
head(bk)
summary(bk)
str(bk) # Very handy function!
head(bk)
# And sale.price.n is numeric, not a factor.
bk$SALE.PRICE.N <- as.numeric(gsub("[^[:digit:]]","", bk$SALE.PRICE))
count(is.na(bk$SALE.PRICE.N))
bk$SALE.PRICE.N
bk$SALE.PRICE.N <- as.numeric(gsub("[^[:digit:]]","RRRR", bk$SALE.PRICE))
bk$SALE.PRICE.N
bk$SALE.PRICE.N <- as.numeric(gsub("[^[:digit:]]","", bk$SALE.PRICE))
bk$SALE.PRICE.N
names(bk)
names(bk) <- tolower(names(bk)) # make all variable names lower case
names(bk)
attach(bk)
hist(sale.price.n)
detach(bk)
bk.sale <- bk[bk$sale.price.n!=0,]
plot(bk.sale$gross.sqft,bk.sale$sale.price.n)
plot(log10(bk.sale$gross.sqft),log10(bk.sale$sale.price.n))
log(10)
bk.sale <- bk[bk$sale.price.n!=0,]
plot(bk.sale$gross.sqft,bk.sale$sale.price.n)
